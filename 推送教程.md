# 造价工作台 · 推送到 GitHub 详细教程

## 一、先搞懂：为什么线上价格同步不了

GitHub Pages 是**纯静态托管，没有后端**。页面上「同步最新价」读的是仓库里的静态文件 `prices.json`，而不是实时去抓行情网站。所以要让线上价格变新，前提是：**仓库里的 `prices.json` 要先被更新并推送上去**。

而你的本地部署源 `E:\zaojia-workbench-github` **根本不是 git 仓库**（文件夹里没有 `.git`），所以之前 `update_prices.ps1` 里的「`git add / commit / push`」整段被跳过——线上 ` prices.json` 永远是第一次提交时的旧数据。顺带，那份 HTML 也是旧版（缺「两行导航」和「母线槽市场铜价」面板）。

**一句话结论**：本地文件夹没连上 GitHub，导致「价格文件没人推、新页面也没上线」。

---

## 二、解决思路（一次性 + 永久自动）

- **一次性**：把 `E:` 文件夹连上你的 GitHub 仓库并推一次（带上最新页面 + 自动更新工作流）。
- **永久自动**：推送后，仓库里的 `.github/workflows/update-prices.yml` 会在 GitHub 服务器上**每天北京时间 18:00 自动**抓取 SMM 电解铜 / 我的钢铁网镀锌板 / 上海金属网母线槽铜价，并自动提交上线。以后**不用你开电脑、不用手动推送**，线上价格自动保持最新。

> 旧方案里的「Windows 计划任务（`register_task.bat` / `update_prices.ps1`）」是用你本机每天 18:00 跑并推送。现在有了 GitHub Actions 在云端跑，**那个本机计划任务就多余了，可以不再使用**（想保留也行，但两者会重复提交，无害）。本文只讲「推一次 + 云端自动」这条路。

---

## 三、准备工作

1. **你的 GitHub 仓库地址**（HTTPS 或 SSH）。
   获取方法：登录 github.com → 打开你的仓库 → 点绿色 **Code** 按钮 → 复制地址，形如：
   `https://github.com/用户名/仓库名.git`
   （或 SSH：`git@github.com:用户名/仓库名.git`）
2. **本机能执行 git 命令**：
   - 已装 **Git for Windows**：直接在终端用 `git` 即可；
   - 没装也没关系：本机已自带便携版 git，下面的「一键脚本」已内置路径，无需安装。
3. **GitHub 登录凭据**（GitHub 自 2021 起**不支持账户密码登录**，推送需用以下任一）：
   - **Git Credential Manager（推荐）**：装 Git for Windows 时默认附带。首次 push 会弹浏览器让你登录 GitHub，记一次即可。
   - **Personal Access Token（备用）**：GitHub → Settings → Developer settings → Personal access tokens → 生成一个带 `repo` 权限的 token。push 时用户名填你的 GitHub 用户名，**密码框填 token**（不是你的账户密码）。
   - **SSH key（最稳，适合后台）**：见文末第九节。

---

## 四、方法一：用我给的「一键推送」脚本（最省事，推荐）

1. 打开 `E:\zaojia-workbench-github\一键推送.bat`（右键 → 用记事本打开）。
2. 找到 `set "REPO_URL=..."` 那一行，把里面的示例地址改成你的**真实仓库地址**，保存。
3. **双击** `一键推送.bat`。
4. 脚本会自动完成：克隆仓库到临时文件夹 → 把 `E:` 里的最新文件复制进去 → 提交 → 推送。
5. 第一次推送会**弹窗请你登录 GitHub**（Git Credential Manager），登录一次即可。
6. 看到「成功！…」即完成。

> 脚本用「先克隆再复制」的方式，自动规避了「本地无 `.git` 直接 push 被拒绝（rejected）」的常见坑。

---

## 五、方法二：自己敲命令（适合想了解过程）

打开终端（PowerShell 或 CMD），逐行执行：

```bat
cd /d E:\zaojia-workbench-github

:: 1) 先克隆你的仓库到一个临时文件夹（避免本地无 .git 导致的冲突）
git clone https://github.com/你的用户名/你的仓库名.git C:\zaojia_tmp_clone

:: 2) 把 E: 里所有最新文件复制进克隆文件夹（覆盖旧文件）
xcopy E:\zaojia-workbench-github\* C:\zaojia_tmp_clone\ /E /Y

:: 3) 进入克隆文件夹，提交并推送
cd /d C:\zaojia_tmp_clone
git config user.name "陈生"
git config user.email "你的邮箱@example.com"
git add -A
git commit -m "更新造价工作台页面与材料价格数据"
git push -u origin HEAD
```

- 推送时若弹窗登录，按提示登录；若提示 `fatal: Authentication failed`，改用 token 或 SSH。
- 若 `git` 提示“不是内部或外部命令”，说明没装 Git for Windows：去 https://git-scm.com 下载安装（默认选项即可），重开终端再试。或者用本机便携 git，把上面所有 `git` 换成：
  `C:\Users\陈生\.workbuddy\binaries\PortableGit\versions\1.2.0\cmd\git.exe`

---

## 六、推送后验证

1. 打开 GitHub 仓库页面，看 **Commits** 是否多了一条你刚提交的记录，且 `zaojia-workbench9.9.html`、`prices.json`、`.github/workflows/update-prices.yml` 都在。
2. 访问你的站点（形如 `https://用户名.github.io/仓库名/zaojia-workbench9.9.html`）。
   等约 1 分钟（GitHub Pages 重新发布），刷新（**Ctrl+Shift+R** 强制刷新）即可看到：
   - 侧边导航变成两行（台账 / 材料价格库 / 清单·定额速查 / 待办事项 / 预留长度 / 电解铜价 / 热镀锌板价 / 母线槽市场铜价 / 使用说明）；
   - 「母线槽市场铜价」面板；
   - 页面取数带缓存破坏，且同步成功会回显「数据更新于 …」。
   - 想用根地址直接打开 → 把文件改名为 `index.html` 再推一次。
3. **立即刷新一次价格**：进仓库 **Actions → 每日更新材料价格 → Run workflow → Run**。
   约几十秒后 `prices.json` 更新，站点三个价格面板即可同步当日价。之后每天 18:00 自动跑。

---

## 七、如果 GitHub Pages 还没开启（一般已开，可跳过）

仓库 **Settings → Pages → Source** 选 **Deploy from a branch** → **Branch** 选 `main`（或你推送的分支）、目录 **/ (root)** → Save。
等 1 分钟，页面顶部会出现站点地址。

---

## 八、常见问题排错

| 现象 | 原因 / 解决 |
|---|---|
| 推送到一半要用户名密码但死活不对 | GitHub 不接受账户密码。改用 token（密码框填 token）或 SSH 地址。 |
| 报错 `rejected` / `non-fast-forward` | 远程已有不同历史。务必用本文「先克隆再复制」的方式（方法一脚本已规避），**不要**直接在 `E:` 里 `git init` 后强推。 |
| `git` 不是内部或外部命令 | 没装 Git for Windows，去 git-scm.com 安装，或用本机便携 git 全路径。 |
| 站点显示旧价 | GitHub Pages 有 CDN 缓存，等 1 分钟或强制刷新（Ctrl+Shift+R）。 |
| 三个价格面板点了「同步」还是旧价 | 那是读「最新已提交的 `prices.json`」。去 Actions 点一次 Run workflow 立即刷新；之后每天 18:00 自动刷新。 |
| 价格抓不到（Actions 日志报错） | 工作流只在抓取成功时才提交；失败则保留上次有效数据。可去 Actions 看运行日志查原因（多为行情站点改版，需改 `price_sync.py` 解析）。 |

---

## 九、（可选）SSH 方式，一劳永逸

如果你经常推送，配一次 SSH 最省心（以后不再弹窗）：
1. 打开终端执行 `ssh-keygen -t ed25519 -C "你的邮箱@example.com"`，一路回车。
2. 打开生成的公钥 `C:\Users\陈生\.ssh\id_ed25519.pub`，复制全部内容。
3. GitHub → Settings → SSH and GPG keys → New SSH key → 粘贴保存。
4. 把 `REPO_URL`（或命令里的地址）换成 SSH 格式：`git@github.com:用户名/仓库名.git`。
5. 再运行推送脚本即可，不再需要每次登录。

---

## 十、推送后日常怎么用

- **改了页面**（比如又加面板）：在 `E:` 改完，再双击一次「一键推送.bat」即可上线。
- **只想刷新价格**：不用动电脑，去仓库 Actions 点 Run workflow，或等每天 18:00 自动跑。
- **本机 `register_task.bat` / `update_prices.ps1` / `run_now.bat`**：在 GitHub Actions 方案下已非必需，可忽略；如仍想用本机方案，需先把 `E:` 变成 git 仓库（即走完本文推送后，`E:` 仍不是仓库，本机计划任务依旧不会推——所以直接依赖云端 Actions 即可）。
