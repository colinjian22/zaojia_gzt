# 造价工作台 · GitHub Pages 部署与价格每日自动更新清单

本清单把 `E:\zaojia-workbench-github\` 这个文件夹完整托管到 GitHub Pages，
并让 **SMM 电解铜价 / 我的钢铁网热镀锌板价** 每天收盘后（18:00）自动重新抓取、自动上线。

---

## 一、当前文件夹里已有哪些文件（无需再动）

| 文件 | 作用 | 是否必需 |
|---|---|---|
| `zaojia-workbench9.9.html` | 工作台主程序（单文件自包含：CSS/JS 全内联，无外部 CDN） | ✅ 必需 |
| `prices.json` | 价格面板预设真实价（电解铜 + 镀锌板 14 城）。面板在 https 下 `fetch('./prices.json')` | ✅ 必需 |
| `price_sync.py` | 抓取脚本：重跑即重新生成 `prices.json`（纯标准库，无需 pip 安装） | ⬜ 维护用 |
| `update_prices.ps1` | **每日更新核心**：跑 `price_sync.py` 重写 `prices.json`；若目录是 git 仓库则自动 `commit + push` | ⬜ 自动更新用 |
| `run_now.bat` | 手动双击立即跑一次 `update_prices.ps1`（先验证用） | ⬜ 调试用 |
| `register_task.bat` | **以管理员身份运行**，登记 Windows 每天 18:00 自动任务 | ⬜ 自动更新用 |
| `.nojekyll` | GitHub Pages 惯例（0 字节），禁用 Jekyll 确保文件原样托管 | ⬜ 建议 |

---

## 二、整体原理（数据流）

```
本机每天 18:00（计划任务）
   └─ update_prices.ps1
        ├─ 调用 price_sync.py  →  抓取 SMM 电解铜 + 我的钢铁网镀锌板
        ├─ 重写 prices.json
        └─ 若当前目录是 git 仓库：
              git add prices.json → commit → push 到 GitHub
                    │
GitHub 收到 push → Pages 自动重新发布（约 1 分钟内生效）
                    │
用户访问 https://<用户名>.github.io/<仓库名>/zaojia-workbench9.9.html
   └─ 价格面板 fetch('./prices.json')  →  显示当日最新价
```

> 关键：**线上站点每天变价的前提 = 本地把 `prices.json` 自动 push 上去**。
> 若文件夹没连 git 仓库，`update_prices.ps1` 只会更新本地文件，线上不会变。

---

## 三、前置条件（先确认）

1. **Python 路径已写死**在 `update_prices.ps1` 第 3 行：
   `C:\Users\陈生\.workbuddy\binaries\python\versions\3.13.12\python.exe`
   若该路径变了，改这一行即可。
2. **Git 已安装**：命令行跑 `git --version` 有输出即可；没有就去 https://git-scm.com 装。
3. **GitHub 账号 + 推送凭据**：
   - 推荐用 **Git Credential Manager**（安装 Git 时默认勾选），首次 push 弹窗登录一次即记住；
   - 或配置 **SSH key**（更稳，适合无头/后台任务）；
   - 注意：GitHub 自 2021 起已**不支持账户密码 push**，必须走 token 或 SSH。
4. **网络**：批量抓取走你本机网络（不受 WorkBuddy 沙箱限制），正常联网即可。

---

## 四、部署步骤

### 步骤 A —— 在 GitHub 建仓库
1. 登录 https://github.com → 右上角 **+ → New repository**。
2. Repository name 任取，例如 `zaojia-workbench`（决定访问地址）。
3. **Visibility**：选 **Public**（GitHub Pages 对公开仓库免费且无需额外设置）。
   - 私有仓库现在也能用 Pages，但要在仓库 Settings → Pages 额外开启，且需要能访问 repo 的账号。
4. **不要**勾选 "Add a README file" / ".gitignore" / "license"（本地已有文件，避免初始冲突）。
5. 点 **Create repository**，进入仓库后复制它的地址：
   - HTTPS：`https://github.com/<用户名>/<仓库名>.git`
   - 或 SSH：`git@github.com:<用户名>/<仓库名>.git`

### 步骤 B —— 本地初始化 git 并连 remote、首次推送
以**管理员或普通用户**打开 PowerShell / CMD，逐行执行（把 `<...>` 换成你的）：

```bat
cd /d E:\zaojia-workbench-github
git init
git add .
git commit -m "init: 造价工作台 + 价格自动更新"
git branch -M main
git remote add origin https://github.com/<用户名>/<仓库名>.git
git push -u origin main
```

- 首次 `push` 会弹窗要你登录 GitHub（GCM），登录一次即可。
- 成功后，这个文件夹就是"连接到你 GitHub 仓库的 git 仓库"了——以后 `update_prices.ps1` 会自动 commit+push。

### 步骤 C —— 登记每日 18:00 自动任务
1. 进 `E:\zaojia-workbench-github\`。
2. **右键 `register_task.bat` → 以管理员身份运行**。
3. 看到 `[成功] 已登记每日任务 ZaojiaPriceDailyUpdate（每天 18:00 运行）` 即完成。
4. 想确认：命令行 `schtasks /query /tn ZaojiaPriceDailyUpdate` 能看到任务。

> ⚠️ **运行时机说明**：默认任务"仅在用户登录时运行"。
> - 若你 18:00 通常开着机并登录 → 正常触发。
> - 若希望**锁屏/注销后也跑**：把 `register_task.bat` 第 5~7 行改成加 `/ru SYSTEM`
>   （进阶，需单独为 SYSTEM 账户配置 git 凭据，一般不推荐，普通使用无需改）。

### 步骤 D —— 开启 GitHub Pages
1. 仓库 **Settings → Pages**（左侧）。
2. **Build and deployment → Source** 选 **Deploy from a branch**。
3. **Branch** 选 `main`，目录选 **`/(root)`**，点 Save。
4. 等约 1 分钟，页面顶部会出现 `Your site is published at https://<用户名>.github.io/<仓库名>/`。
5. 访问：`https://<用户名>.github.io/<仓库名>/zaojia-workbench9.9.html`
   - 想用根地址直接打开 → 把文件**改名为 `index.html`** 再 push 一次即可。

---

## 五、验证是否跑通

1. **手动先跑一次**：双击 `run_now.bat`。
2. 看 `E:\zaojia-workbench-github\update_prices.log`：
   - 应包含 `price_sync.py 执行完成` 和 `已尝试提交并推送到 GitHub`（若已连 git）。
3. 去 GitHub 仓库看 `prices.json` 的提交时间是否更新为今天。
4. 访问站点，价格面板是否显示最新价（电解铜/镀锌板）。
5. 次日 18:00 后重复第 2~4 步确认自动触发。

---

## 六、常见问题排错

| 现象 | 原因 / 解决 |
|---|---|
| `register_task.bat` 报失败 | 没用**管理员身份**运行。右键 → 以管理员身份运行。 |
| 线上价格不变 | 文件夹没连 git（`update_prices.log` 会写"当前目录不是 git 仓库"）。回到步骤 B。 |
| push 时卡住/要求输入 | 没配凭据。装 Git Credential Manager 登录一次，或用 SSH key；脚本已设 `GIT_TERMINAL_PROMPT=0` 防卡死。 |
| 任务到点没跑 | 18:00 电脑已注销（默认仅登录时运行）；或 Python 路径变了（改 `update_prices.ps1` 第 3 行）。 |
| 价格抓不到（SMM/我的钢铁网改版） | 站点结构变了，`price_sync.py` 正则失效。重跑脚本看报错，按源码注释调整解析。 |
| 站点显示旧价 | GitHub Pages 有 CDN 缓存（通常 1 分钟内，偶发更久）。等一会或浏览器强制刷新（Ctrl+Shift+R）。 |

---

## 七、日常维护提示

- **改更新时间**：编辑 `register_task.bat` 的 `/st 18:00`（如 `/st 20:30`），重新以管理员运行即可覆盖。
- **暂停自动更新**：`schtasks /change /tn ZaojiaPriceDailyUpdate /disable`；恢复把 `/disable` 换成 `/enable`。
- **彻底删除任务**：`schtasks /delete /tn ZaojiaPriceDailyUpdate /f`。
- **手动改价后上线**：直接 `git add prices.json && git commit -m "手动更新" && git push`。
- 日志文件 `update_prices.log` 不在 git 跟踪内（脚本只 `git add prices.json`），不会污染提交。

---

## 八、安全与合规提醒

- SMM 电解铜价取自 `www.smm.com.cn/price` 公开现货表；镀锌板取自我的钢铁网 `duxin.mysteel.com` 公开页。
- 价格仅供内部参考，正式报价请以各官网当日公布为准。
- 工作台数据保存在浏览器 `localStorage`，换机器用「一键备份 / 导入恢复」带走；与 `prices.json` 是两回事（后者只管价格面板）。
